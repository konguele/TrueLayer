1- Generally, the most common error is the environment variables or not having set the absolute paths. Possibly having write logger instead of /usr/bin/logger could cause crontab not to recognize it well. In either case, it is always a good idea to be able to reproduce the error to determine the exact root cause.

2- The two main families within Unix-based systems, such as Linux, are AT&T System V and Barkeley's BSD.
If we focus exclusively on Linux, we see that they come from the Debian, Slackware and RedHat families.

3- cat EOF is also known as Heredoc and its purpose is to use a form of I/O redirection to feed a command list to an interactive program or a command.

4- File exercise4.sh

5- the easiest way to do this is by using the ssh-copy-id command. In the terminal we must write ssh-copy-id user@server